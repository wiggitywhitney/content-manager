#!/usr/bin/env node

/**
 * Simple test script to verify the development environment is set up correctly.
 * This script validates that Node.js can execute and basic imports work.
 */

console.log('Hello from content-manager!');
console.log('Environment setup verified successfully.');
console.log(`Node version: ${process.version}`);
