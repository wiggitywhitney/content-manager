{
  "name": "content-manager",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sync": "teller run -- $(which node) src/sync-content.js",
    "sync:test": "BLUESKY_HANDLE=$(grep BLUESKY_HANDLE .env | cut -d= -f2) DRY_RUN=true LOG_LEVEL=DEBUG teller run -- $(which node) src/sync-content.js",
    "delete-old-posts": "teller run -- $(which node) src/delete-old-posts.js",
    "check-posts": "teller run -- $(which node) src/check-posts.js",
    "add-test-rows": "teller run -- $(which node) src/add-test-rows.js",
    "remove-test-rows": "teller run -- $(which node) src/remove-test-rows.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0"
  },
  "type": "commonjs",
  "devDependencies": {
    "commit-story": "file:../commit_story/commit-story-1.2.1.tgz"
  },
  "dependencies": {
    "@atproto/api": "^0.12.0",
    "dotenv": "^17.2.3",
    "googleapis": "^164.0.0"
  }
}
